# Repository Guidelines

## Project Structure & Module Organization

This is a documentation-first workspace for verifiable A-share trading systems.

- `产业卡点及事件驱动系统/` is active. Its numbered folders cover requirements (`01_需求`), research (`02_研究`), rules (`03_规则与规格`), data (`04_数据`), implementation (`05_实现`), validation (`06_测试与验证`), and operations (`07_运行与复盘`).
- `题材扩散与资金轮动系统/` is a separate research project with the same numbered layout. Do not mix its signals, P&L attribution, or rules into the industrial-event system.
- `原始文档/` contains user-supplied sources. Treat it as an immutable baseline; put derived analysis in the relevant project.
- `归档/` contains historical outputs and screenshots. Use it for traceability, not as the current specification.

Read the root `README.md`, the relevant project `README.md`, and its PRD when one exists before changing downstream artifacts.

## Development & Validation Commands

The Stage 1 engineering skeleton uses Python 3.12, setuptools, hash-locked dependencies, pytest, Ruff, and GitHub Actions. Use these repository-level checks from PowerShell:

```powershell
& "E:\Conda\envs\Data_Analysis\python.exe" -m pytest -q
& "E:\Conda\envs\Data_Analysis\python.exe" -m ruff check .
& "E:\Conda\envs\Data_Analysis\python.exe" -m ruff format --check .
& "E:\Conda\envs\Data_Analysis\python.exe" -m mypy
& "E:\Conda\envs\Data_Analysis\python.exe" -m compileall -q src tests
git diff --check
```

Generate reproducible locks with `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\compile-locks.ps1`. Do not install the full dev lock into the shared environment when that would change existing packages. Document implementation tooling in the relevant project's `05_实现/README.md`.

## Writing Style & Naming Conventions

Write UTF-8 Markdown with one descriptive H1, short sections, and concise lists or tables. Preserve Chinese domain terminology; wrap states, fields, and commands in backticks. Name versioned artifacts clearly, for example `来源登记表_v0.2.md`. Record evidence revisions through versioning or `supersedes` lineage. Only `approved` rules are backtest-ready; `draft`, `hypothesis`, and `placeholder` values are non-production.

## Testing Guidelines

Place validation plans, golden cases, replay results, and bias checks under `06_测试与验证/`. Every rule change should identify evidence, point-in-time assumptions, success and failure cases, and an `ABSTAIN` case. Check internal links and citations manually until automation is added.

## Commit & Pull Request Guidelines

Git history is unavailable, so no existing convention can be inferred. Use short, imperative subjects such as `docs: clarify PIT evidence rules`. Keep commits focused. Pull requests should summarize the decision, list affected folders, link evidence or issues, identify status/version changes, and report validation. Add screenshots for visual-output changes.

## Security & Operational Boundaries

Never commit credentials, broker tokens, or account data. Research agents must not access trading credentials or submit orders. Live outputs require a data snapshot, rule version, test report, and human approval record.

## Cross-Repository Isolation

`InvestmentResearchKB` is an independent provider. This repository may consume only pinned, versioned public contracts and exact Published Release artifacts. Implementation, tests, and CI must not read the sibling repository's SQLite, `raw/`, `staging/`, `published/`, source package, worktree, or temporary directories; do not add sibling-path imports, the KB editable install as a dependency, submodules, symlinks, junctions, shared writable caches, databases, migrations, project runtimes, or CI checkouts.

The approved local-development exception is the workstation Conda environment `E:\Conda\envs\Data_Analysis` with Python 3.12. Treat it only as a shared interpreter, not as the dependency contract: InvestSystem owns its `pyproject.toml`, hash-locked runtime/dev requirements, TOML configuration, cache, database, and runtime directory. Register this project with editable `--no-deps`; never import the KB package from the shared environment. Before and after adding an InvestSystem package, record the environment baseline and run `pip check`; install only exact locked missing packages without changing existing shared packages unless the user separately approves that impact. CI and reproducible validation must build an isolated environment from the InvestSystem lock.

KB credentials must be read-only and limited to the published delivery surface. Preserve `origin` as the writable fork remote, never push to `upstream`, and establish and verify a technical no-push guard on `upstream` during Stage 0 before that stage can complete.

ADR-0001 is approved. The first version permits exactly one `strategy_input_ref` per run and supports both versioned read-only HTTP API and authorized immutable export packages. InvestSystem owns `var/state/invest_system.sqlite3` and the content-addressed cache at `var/cache/kb-releases/`; the cache has a 20 GiB soft limit, and historically referenced artifacts must never be automatically deleted. A withdrawn or unconfirmable Release blocks every new run. Preserved historical material may be used only for explicitly labeled `audit_replay`, never for a new current decision, position, approval, or order.

The current clone's `upstream` push URL uses the prohibited `disabled` transport and `remote.pushDefault=origin`. This is clone-local: verify and reapply the guard in every new clone before development.

Industrial-event and theme-rotation strategies default to zero signal interchange. They may separately pin the same KB Release, read the same provider-neutral immutable content-addressed Release cache, and reuse provider-neutral execution or market-rule libraries, but each must own its input references, consumption receipt/observations, Manifest, states, rules, ledger, attribution, and P&L. Any future cross-strategy feature requires an approved ADR, a versioned contract, and revalidation of every affected strategy.

As of 2026-08-12, Stage 3 is `completed_with_scope_limits`. Stage 3A completed offline Client/export contract validation at KB commit `2c84277ef463b5dd9a3fda3f2976a30cade53af5`; Stage 3B repinned the transport snapshot to KB commit `aab36fe229104779b50ec71e2dc37a9fad81d285` and passed independent-process loopback HTTP compatibility; Stage 3C passed real tcloud public-HTTPS validation for the exact formal `market_daily` Release; Stage 3D passed real public-HTTPS Context Pack/Evidence validation, provider-neutral mapping and a validation-only `ABSTAIN` strategy smoke. Stages 3B—3D grant no current-status authority, issue no `RunReleaseStatusConfirmation`, and persist no run state. Historical-validation admission, authenticated status evidence, run-scoped confirmation and atomic persistence are Stage 6 gates; current shadow/paper admission is a separate Stage 7 gate. Do not reopen Stage 3 to add those capabilities, and never derive them from the Stage 3 validation-only objects. Never use a KB worktree, internal package, mock, or same-process TestClient as evidence for 3B—3D. Stage 4 is `completed_with_scope_limits`: 4A-1—4A-4 and the independent 4B complete orchestration are implemented only in their exact approved `stage4_synthetic_research_validation` scope. The 4B engine must recompute every local result from raw typed case inputs; caller-supplied partial results, real KB Releases, KB current-status authority, real/executable prices, and real holdings remain forbidden. Never reuse the Stage 2B capability or a local 4A capability as the 4B capability, and do not implement PRD `hypothesis`/`TBD` values as defaults.

Stage 5A rule governance is owner-approved only for the exact `stage5_synthetic_execution_validation` scope. Preserve the original specification and zero-authority draft proposal unchanged; the separate approved bundle, approval record, and `stage5_governance.py` may issue only exact anonymous synthetic `research` validation capabilities. Stage 5B is `completed_with_scope_limits`: `stage5_market_execution.py` implements content-addressed historical market rules, calendars, first-executable-window selection, costs, monotonic impact, executable-price Gate 3/4 rechecks, DAY synthetic intents/fills/cancellations, and deterministic in-memory replay. It must bind the same-run raw Stage 4 case to the exact Stage 4 result/input/replay and must never treat proposal reference prices as executable. Preserve the standalone Stage 5B v0.1 path; Stage 5C constrained execution must use the two-phase candidate → reduction-only constraint → finalize seam and must not relabel portfolio-unapproved or unsubmitted quantity as an order cancellation.

Stage 5C is `completed_with_scope_limits`: the Stage 5C contracts/risk/engine/fill/ledger modules implement anonymous synthetic account and recovery records, all-cluster portfolio risk and five quantity layers, pre-submit cash/cost-reserve/sellable constraints, content-bound settlement/availability projections, and an as-of in-memory append-only double-entry/FIFO journal. Future settlement or availability events must never leak into the current derived state. Non-empty corporate actions, marks, NAV/P&L, external cash flow, global fee-capitalized lot-cost reconciliation, SQLite migrations, durable atomic persistence, complete Stage 5 replay, and the golden matrix remain Stage 5D and are not implemented. Backtest, paper, shadow, live, real positions/accounts/orders, broker connectivity, funds deployment, and KB writes remain unauthorized.

Stage 5D rule governance is owner-approved only for the exact `stage5_synthetic_execution_validation` scope. Preserve the 48-item specification and zero-authority draft machine proposal byte-for-byte; the separate approved bundle, approval record, approval document, and `stage5d_governance.py` may issue only the exact anonymous synthetic `research` validation capability. The approval lineage is not a business implementation. Bounded `5D-1` is `completed_with_scope_limits` only for the first pre-registered order/contract historical replay in `docs/validation/stage5d-first-order-contract-replay-acceptance.md`: the fixed ENTER/BUY case recomputes Stage 5C in the same call, rematerializes the opening, replays the five source-driven Ledger V2 events, and produces the fixed mark/NAV, approved 18-cell P&L and deterministic complete/audit replay. The frozen case, horizon, event inventory, support matrix and accounting golden in `docs/validation/stage5d-first-order-contract-replay-preregistration.md` must not change after observing results. The 48 approved items remain the long-term governance ceiling; SELL, non-empty corporate actions, external cash flows and full securities-accounting coverage are not implemented by this bounded acceptance. Unsupported inputs must fail closed as `BLOCKED`/`ABSTAIN` before publishing partial NAV/P&L; never silently ignore, approximate or post-select them. Durable `5D-2` remains `currently_authorized=false`. SQLite schema v4, migrations, persistence, backtest, paper, shadow, live, real positions/accounts/orders, broker connectivity, funds deployment, and KB writes remain unauthorized. Preserve Stage 5A and Stage 5C historical artifacts unchanged.

Stage 6A historical-validation governance and Stage 6B admission governance are owner-approved. Stage 6B is `completed_with_scope_limits` only for the isolated validation store and the real public-HTTPS validation-only seal recorded in `docs/validation/stage6b-live-https-acceptance.md`; that seal is not formal historical-run authority. Stage 6C v0.2 governance is owner-approved only for implementing and validating an anonymous synthetic holdout-isolation/candidate/fold/statistics/replay kernel. Preserve the v0.1 and v0.2 draft bytes and use only the fully materialized approved v0.2 bundle through `stage6c_governance.py`. The six approved anonymous synthetic slices, culminating in `stage6c_phase.py`, are `completed_with_scope_limits` and accepted in `docs/validation/stage6c-synthetic-phase-seal-acceptance.md`. `SYNTHETIC_PHASE_SEALED` proves only zero-I/O kernel orchestration and must always carry `not_ready_for_6d_freeze=true`; it is not a formal Stage 6C result or real 6D request. Do not add more synthetic scope merely to bypass missing real evidence. Formal 6C remains blocked until separately approved historical execution, an exact formal admission seal/migration, PIT data readiness, sufficient Stage 5D and peer benchmark support, real experiment/audit sources, and real holdout custody/ACL/canary/zero-read evidence all exist. The 2026 partition is only a locked historical holdout; Stage 7 is still required for truly unknown evidence.
The exact blocked baseline is `docs/validation/stage6c-formal-execution-readiness-audit-v0.1.md`: it permits only public-delivery readiness census, outcome-blind Stage 5D support census, architecture design, and owner review. Do not issue formal authority or start development/walk-forward execution from the audit itself.
The IS-only static support baseline is `docs/validation/stage5d-outcome-blind-static-support-census-v0.1.md`. It proves source-driven SELL ledger/FIFO/settlement components exist, but not generalized SELL complete replay, daily NAV/P&L, real candidate coverage, or completed-trade eligibility. Keep all real coverage metrics unknown until a separate content-addressed candidate census is frozen before outcomes; the next permitted design artifact is a preregistered ordinary full-EXIT complete-replay slice.
The exact-input normal lifecycle evaluator is now accepted in `docs/validation/stage5d-normal-lifecycle-evaluator-acceptance.md`. It consumes only input-set `60cad84334d110e910be897179bf76409f59c59b3bcd45fd733d29cf67829e30`, preserves historical BUY/partial-SELL bytes, and produces one anonymous synthetic full-EXIT completed-trade record plus a 61-point valuation series. Never count it toward real Stage 6 completion, infer candidate coverage, add benchmark shortcuts, or extend it to company actions/persistence/formal runs without separate evidence and authorization.
Stage 6 KB historical handoff acceptance is governed by `docs/validation/stage6-kb-historical-handoff-acceptance-runbook-v0.1.md`. The runbook currently grants no parser, network, candidate, coverage, persistence, or run authority. Do not invent the producer's future Schema; wait for an exact owner-provided handoff, validate only public HTTPS/export bytes, and fail closed on any 2026 holdout content or proxy. Windows CI requires the directly hash-locked `tzdata` dependency so `ZoneInfo("Asia/Shanghai")` does not depend on runner system data.
The minimum provider-neutral requirements are owner-approved in `docs/validation/stage6-minimum-public-data-consumption-contract-v0.2.md`; preserve the v0.1 draft bytes for lineage. `S6DATA-01..10` are approved atomically, with Beta benchmark fixed to CSI `H00985` and no fallback. This approval only freezes the future KB delivery requirements and governance lineage: it does not authorize real handoff, KB backfill, an IS parser, candidate projection, coverage, historical run, migration, holdout access, or any trading mode. Preserve the one-root `strategy_input_ref` boundary even if KB uses multiple immutable source-family Releases. Repin only after the future root Release candidate and producer validation, before any real handoff token.
ADR-0002 and the Stage 6 consumer profile v0.3 exact draft bytes are owner-approved through `docs/validation/stage6-provider-consumer-boundary-approval-v0.1.md`; preserve the pending drafts as immutable approval material. KB owns generic Release/Manifest/Status/Artifact/Schema/Context Pack, PIT facts, extensible benchmark/factor registries, corporate-action/industry ontologies and variable dependency closure. IS constructs `StrategyInputRef`, selects H00985, accepts/recomputes ADV20/Beta120 and owns holdout, candidate/coverage, authority and run permissions. This governance approval does not authorize KB Schema implementation, data work, IS Adapter implementation, repin, handoff or any run. Preserve all v0.1/v0.2 bytes and legacy provider snapshots; no silent migration is permitted.
The separately authorized Stage 6 generic offline Adapter slice pins exactly 19 Git-object files from KB merge commit `4352c10c6c639e25d4c190dfc9ec58ee9e76aa86` under `contracts/providers/investment_research_kb/stage6-provider-contracts-v1/`. Runtime/tests may read only this InvestSystem-owned snapshot. The slice validates draft Schemas/catalog/registry/fixture, projects ReleaseReference/dependency closure/data profile, selects H00985, validates ADV20/Beta120 definitions, and provides pure exact-window recomputation helpers. H00985 real acceptance remains blocked on permission/PIT; the official fixture has basis hashes but no raw records, so it must never be reported as an official numeric recomputation. No transport repin, network, real Release, handoff, candidate, coverage, historical run, holdout or trading authority is granted.
The industrial-event PRD v0.4 boundary addendum is a pending-owner draft only. Preserve the approved v0.3 bytes exactly. The draft may supersede only the listed boundary clauses: IS constructs `StrategyInputRef` from a verified KB ReleaseReference, Manifest verification precedes projection, ownership follows ADR-0002, and C1a draft compatibility is separate from C1b Published Release consumption. Do not treat the draft as approval for Stage 6B refactoring, spike cleanup, data work, repin, handoff or any run.
