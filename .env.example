# Optional local override. Copy to .env only when local tooling needs it.
# Never place credentials, broker tokens, account data, or KB write access here.
INVEST_SYSTEM_CONFIG_PATH=config/default.toml
