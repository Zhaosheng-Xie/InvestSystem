# 规则与规格

当前状态：`deferred / not_started`。只有经过本项目需求确认和数据审计的状态、信号、退出、归因与风控规则才能写入本目录。`hypothesis` 不得用于实盘；进入回测前必须明确版本和反例。

题材规则必须独立维护状态机、版本、参数、候选、账本、归因和 P&L，不得导入产业策略的信号或 Gate 结果，也不得默认向产业策略输出 `theme_regime`、`crowding_state` 等策略状态。未来若要建立跨策略输入，必须先通过单独 ADR、版本化契约和受影响策略的重新验证。
