# 实现

当前状态：`deferred / not_started`。未来实现包括题材发现、状态回放、成交仿真、回测和影子账本。本项目不依赖产业系统 Stage 7 成功，也不会随产业项目进度自动启动。

实现必须使用独立的输入引用、消费回执、Manifest、策略状态、规则、账本和 P&L。仅可复用不含任何策略信号、标签、Gate 判断或 Alpha 归因的纯执行/市场规则库，以及 provider-neutral、内容寻址且不可变的只读 Release 缓存；不得共享 raw、staging、SQLite、可变策略缓存或运行目录，也不得导入产业系统的入场资格和决策状态。
